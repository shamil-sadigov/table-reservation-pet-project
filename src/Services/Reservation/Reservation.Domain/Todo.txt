- Ensure in domain rules we don't specify in error parameter names
- Figure out if domain events should be raised in aggregate root only or it's acceptable to be raised in nested entities as well
- Add test that check restaurant without available tables
- Override has all ToString() in value objects that contains one value
- Add SingleValueObject<T> : ValueObject that will contains only single value object
- Regarding Unit tests: Instead of creating necessary objects in local method during 'Act' step, 
it's better to retrieve creation logic in separate Builder pattern and reuse this builder 




TODO: regarding domain
Customer
- Add PhoneNumber in Customer




ReservationRequest
- When creating Reservation based on ReservationRequest, ReservationRequest should ensure time is valid


ADD integration tests